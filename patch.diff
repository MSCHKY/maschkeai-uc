--- src/style.css
+++ src/style.css
@@ -858,6 +858,18 @@
     caret-color: var(--terminal-accent);
 }

+/* Accessibility: Focus visible styles for keyboard navigation */
+*:focus-visible {
+    outline: 2px solid var(--terminal-accent);
+    outline-offset: 2px;
+}
+
+/* Highlight the container when the input is focused */
+#terminal-input-line:focus-within {
+    border-color: var(--terminal-accent);
+    box-shadow: 0 0 0 1px rgba(35, 92, 255, 0.15);
+}
+
 /* Custom cursor (hidden, we use caret-color instead) */
 #cursor {
     display: none;
